<UserControl x:Class="CarApp.CarView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:CarApp"
             Height="Auto" Width="Auto">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="Auto" />
        </Grid.ColumnDefinitions>

        <TextBlock Text="{Binding Model}" Grid.Column="0" Margin="5" />
        <TextBlock Text="{Binding Brand}" Grid.Column="1" Margin="5" />
        <TextBlock Text="{Binding Price}" Grid.Column="2" Margin="5" />
    </Grid>
</UserControl>